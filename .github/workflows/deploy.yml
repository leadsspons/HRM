name: Deploy to Hostinger
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy web/ over SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.5
        with:
          server: 72.60.128.99
          port: 65002
          username: u547008780_UAkCmKQPC
          password: ${{ secrets.SFTP_PASSWORD }}
          local_path: ./web/*
          remote_path: /home/u547008780/websites/UAkCmKQPC/public_html
          exclude_list: |
            config.php
            service_account.json
